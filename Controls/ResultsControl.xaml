<UserControl x:Class="LinearCutWpf.Controls.ResultsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Результаты раскроя" FontSize="18" FontWeight="Bold"/>
            <TextBlock x:Name="txtTotalStats" FontSize="14" Margin="0,5,0,0" Foreground="DarkSlateGray"/>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="resultsItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="LightGray" BorderThickness="1" CornerRadius="3" Margin="0,0,0,15" Padding="10">
                            <StackPanel>
                                <TextBlock Text="{Binding GroupKey}" FontWeight="Bold" FontSize="14" Margin="0,0,0,5"/>
                                
                                <DataGrid ItemsSource="{Binding ResultTable}" 
                                          AutoGenerateColumns="True" 
                                          IsReadOnly="True" 
                                          Background="White"
                                          HeadersVisibility="Column"
                                          HorizontalGridLinesBrush="LightGray"
                                          VerticalGridLinesBrush="LightGray"
                                          Margin="0,0,0,10"/>
                                          
                                <Border Background="#F8F8F8" Padding="15" CornerRadius="4" HorizontalAlignment="Right">
                                    <StackPanel HorizontalAlignment="Left">
                                        <TextBlock Text="{Binding StatsParts}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding StatsStocks}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding StatsRemainders}" Margin="0,0,0,4"/>
                                        <TextBlock Text="{Binding StatsKpd}" FontWeight="Medium"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
