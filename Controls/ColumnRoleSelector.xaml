<UserControl x:Class="LinearCutWpf.Controls.ColumnRoleSelector"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="28" d:DesignWidth="120">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="22"/>
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="tbColumnName" Grid.Column="0" VerticalAlignment="Center"
                   TextTrimming="CharacterEllipsis" Padding="2,0" FontSize="11"
                   FontWeight="Bold" FontStyle="Italic"/>

        <ToggleButton x:Name="btnToggle" Grid.Column="1" Width="20" Content="▼" Padding="0"
                      HorizontalContentAlignment="Center" VerticalContentAlignment="Center"
                      FontSize="8"/>

        <Popup x:Name="popup" Grid.ColumnSpan="2" Placement="Bottom" PlacementTarget="{Binding ElementName=btnToggle}"
               StaysOpen="False">
            <Border Background="White" BorderBrush="Gray" BorderThickness="1" MaxHeight="200">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="itemsPanel"/>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
